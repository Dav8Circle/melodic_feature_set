# Superseded by deploy-docs.yml (Sphinx site includes the feature catalogue).
# Kept as a no-op redirect of the workflow filename so existing badge/links
# that reference this path do not 404; remove once Pages cutover is complete.

name: Deploy Interactive Features Table to GitHub Pages (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: |
          echo "This workflow is deprecated. Documentation (including the"
          echo "interactive feature catalogue) is deployed by deploy-docs.yml."
          exit 0
